import { TextShorthandPipe } from './text-shorthand.pipe';

describe('TextShorthandPipe', () => {
  it('create an instance', () => {
    const pipe = new TextShorthandPipe();
    expect(pipe).toBeTruthy();
  });
});
